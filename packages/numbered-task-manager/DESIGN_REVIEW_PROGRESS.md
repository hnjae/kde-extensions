# Design Review Progress

<!-- SPDX-FileCopyrightText: 2026 KIM Hyunjae -->
<!-- SPDX-License-Identifier: AGPL-3.0-or-later -->

## Completed

- P1 model-index shape invariant: `TaskEntryLogic.mjs` now owns explicit `modelIndexState(...)` classification and actionability helpers. Diagnostics and action boundaries consume the named policy, preserving current unknown-shape actionability while no longer treating that case as simply valid.
- P1 disabled or invisible context-menu actions remain executable: `TaskContextMenuRouteLogic.mjs` now routes descriptors with explicit `enabled: false` or `visible: false` to unavailable routes, and `TaskContextMenuActionDispatcher.qml` converts those routes into structured dispatch failures before any launcher, launcher-activity, or task-model effect can run. Missing availability fields remain actionable to preserve existing submenu leaf behavior.
- P1/P2 virtual desktop ownership: `VirtualDesktopLogic.mjs` now owns desktop identity coercion, desktop-list membership, current/remote desktop qualification, and context-menu checked-state primitives. `TaskScopeLogic.mjs` and `TaskContextMenuVirtualDesktopLogic.mjs` consume the shared owner, and `TaskEntryLogic.mjs` no longer carries virtual-desktop membership policy.
- P1/P2 context-menu task command dispatch: `TaskActionLogic.mjs` now validates context-menu task request names against an explicit supported-method allowlist, and `TaskContextMenuTaskCommandAdapter.qml` executes supported `TasksModel.request*` calls through explicit branches instead of dynamic `taskModel[result.requestMethod]` dispatch. The broader raw `TasksModel` port finding remains open for launcher sync and context-menu role/opening boundaries.
- P1 context-menu role snapshot ownership: `TaskContextMenuRoleLogic.mjs` now owns context-menu live role reads and snapshot construction. `TaskContextMenuRoleState.qml` consumes the focused owner directly. Broader context-menu feature-family extraction remains open.
- P1 context-menu launcher-activity ownership: `TaskContextMenuLauncherActivityLogic.mjs` now owns launcher-activity menu state and update descriptors. `TaskContextMenuActionSectionsLogic.mjs` consumes the focused owner for aggregate menu composition, and `TaskContextMenuLauncherActivityAdapter.qml` imports the focused owner directly. Broader context-menu feature-family extraction remains open.
- P1 context-menu task-activity ownership: `TaskContextMenuTaskActivityLogic.mjs` now owns task-activity menu state and action descriptors. `TaskContextMenuActionSectionsLogic.mjs` consumes the focused owner for aggregate menu composition. Broader context-menu feature-family extraction remains open.
- P1 context-menu window-action ownership: `TaskContextMenuWindowActionLogic.mjs` now owns window-task action descriptor policy for new instance, move/resize, minimize/maximize, keep above/below, fullscreen/shade/no-border, capture, and close actions. `TaskContextMenuActionSectionsLogic.mjs` consumes the focused owner for aggregate menu composition. Broader context-menu feature-family extraction remains open for footer actions.
- P1 context-menu virtual-desktop action ownership: `TaskContextMenuVirtualDesktopLogic.mjs` now owns virtual-desktop action descriptor policy for submenu availability, all-desktops and per-desktop checked state, new-desktop availability, command descriptors, labels, icon names, and section composition. `TaskContextMenuActionSectionsLogic.mjs` consumes the focused owner for aggregate menu composition.
- P1 context-menu pin-action ownership: `TaskContextMenuPinLogic.mjs` now owns Pin/Unpin state, launcher command descriptors, action descriptors, pin section composition, and the menu-facing launcher pin-state snapshot. `TaskContextMenuActionSectionsLogic.mjs` consumes the focused owner for aggregate menu composition. `TaskContextMenuLauncherState.qml` imports the focused owner directly while launcher-list membership remains in `LauncherListLogic.mjs`.
- P1/P2 context-menu route ownership: `TaskContextMenuRouteLogic.mjs` now owns route classification, route-kind constants, command-kind constants used by route classification, and route-kind predicates. `TaskContextMenuActionDispatcher.qml` consumes that owner directly.
- P1 context-menu final action-section composition ownership: `TaskContextMenuActionSectionsLogic.mjs` now owns aggregate context-menu action-section assembly from focused window-action, pin-action, launcher-activity, task-activity, and virtual-desktop action owners. `TaskContextMenuLogic.mjs` keeps the QML-visible action-section facade and platform snapshot helpers.
- P1 context-menu compatibility re-export cleanup: `TaskContextMenuLogic.mjs` no longer re-exports focused route, role, launcher-activity, pin, task-activity, virtual-desktop, or window-action owners. Tests import focused owners directly while runtime QML continues to use the facade only for action-section assembly, platform entry snapshots, and panel placement. Footer actions remain open.
- P1 launcher sync orchestration: `LauncherSyncLogic.mjs` now owns `persistLaunchers`, `applyLauncherList`, and launcher-list reconciliation sequencing through explicit effect ports. `LauncherSyncAdapter.qml` is a wrapper around real QML model/configuration ports, and executable tests cover successful writes, thrown writes, convergence mismatches, retry, clear, and expiry paths. Broader launcher-list domain/sync separation remains open because `LauncherListLogic.mjs` still carries lower-level sync helpers during migration.
- P1 broad raw `TasksModel` port, launcher command boundary: `LauncherCommandAdapter.qml` now consumes `LauncherCommandPort.qml` for launcher-list reads and add/remove launcher requests instead of receiving the full `TasksModel`. Broader raw model port work remains open for launcher sync and context-menu role/opening boundaries.
- P1 broad raw `TasksModel` port, context-menu task command boundary: `TaskContextMenuTaskCommandAdapter.qml` now consumes `TaskCommandPort.qml` for supported context-menu task request execution instead of receiving the full `TasksModel`. Broader raw model port work remains open for launcher sync and context-menu role/opening boundaries.
- P1 broad raw `TasksModel` port, context-menu launcher read boundary: `LauncherReadPort.qml` now exposes launcher-list, launcher-position, and launcher-activity reads to `TaskContextMenu.qml`, `TaskContextMenuLauncherState.qml`, and `TaskContextMenuLauncherActivityAdapter.qml` instead of passing the full `TasksModel` as a launcher model. Broader raw model port work remains open for launcher sync and context-menu role/opening boundaries.
- P1 broad raw `TasksModel` port, activation boundary: `TaskActivationAdapter.qml` now consumes `TaskActivationPort.qml` for normal task activation instead of receiving the full root `TasksModel`. Broader raw model port work remains open for launcher sync and context-menu role/opening boundaries.
- P1 broad raw `TasksModel` port, task movement boundary: `TaskMoveAdapter.qml` now consumes `TaskMovePort.qml` for launcher-list reads and launcher-position lookups instead of receiving the full root `TasksModel`. Broader raw model port work remains open for launcher sync and context-menu role/opening boundaries.
- P2 numbered slot limit: centralized normal slot limit, Meta+0 shortcut index, slot assignment, and slot-label validation in `TaskNumberingLogic.mjs`; architecture intent and focused shared-owner tests added. Verified with `devenv shell -- devenv tasks run --mode single ci` (unavailable: missing `devenv.nix`), `just check`, `just test-host`, and `just lint-host`. Commit: 68af460.
- P2 task source and visible-item kind strings: centralized normal and remote-attention visible item identities and predicates in `VisibleTaskItemsLogic.mjs`; activation validation, activation routing, and source diagnostics consume the shared owner. Verified with `devenv shell -- devenv tasks run --mode single ci` (unavailable: missing `devenv.nix`), `just check`, `just test-host`, and `just lint-host`. Commit: f18da56.
- P2 context-menu command descriptors: moved launcher and task command descriptor factories into `TaskContextMenuCommandLogic.mjs`; focused menu feature modules consume the command owner while `TaskActionLogic.mjs` keeps dispatch/result classification. Verified with `devenv shell -- devenv tasks run --mode single ci` (unavailable: missing `devenv.nix`), `just check`, `just test-host`, and `just lint-host`. Commit: 40cc97f, ff80244.
- P1/P2 virtual desktop backlog reconciliation: removed stale completed virtual-desktop identity findings and roadmap references from `DESIGN_REVIEW_CORRECT_END_STATE.md`; verified current ownership with targeted virtual desktop, task scope, and context-menu virtual desktop tests. Verified with `node tests/virtualdesktoplogic.test.mjs && node tests/taskscopelogic.test.mjs && node tests/taskcontextmenuvirtualdesktoplogic.test.mjs`. Commit: a8d49ab.
- P1 action boundary backlog reconciliation: removed stale completed disabled context-menu dispatch and model-index shape findings plus roadmap references from `DESIGN_REVIEW_CORRECT_END_STATE.md`; verified current route rejection and model-index policy with targeted route, context-menu, task-entry, and action-result tests. Verified with `node tests/taskentrylogic.test.mjs && node tests/taskactionlogic.test.mjs && node tests/taskcontextmenuroutelogic.test.mjs && node tests/taskcontextmenulogic.test.mjs`. Commit: b9f30c3.
- P2 visible descriptor activation invariant: `VisibleTaskItemsLogic.mjs` now owns visible-item descriptor validation and shortcut activation rejects mismatched descriptor state before activation routing; tests cover descriptor validation and activation rejection. Verified with `node tests/visibletaskitemslogic.test.mjs && node tests/taskactionlogic.test.mjs && node tests/taskactivationadapterqml.test.mjs`, `just check`, `just test-host`, and `just lint-host`. Commit: e7b82bc, d73576b.
- P2 stale drag move diagnostics: `TaskMoveAdapter.qml` now recomputes a typed move decision and emits structured drag move rejection results when source or target entries disappear after the initial move check; adapter tests cover the stale branch. Verified with `node tests/taskmoveadapterqml.test.mjs && node tests/taskactionlogic.test.mjs && node tests/taskmodellogic.test.mjs`, `just check`, `just test-host`, and `just lint-host`. Commit: e83782d, 9df8875.
- P1 structured launcher command outcome: launcher pin/unpin persistence success and failure now return action-shaped results with launcher action and sync context, and `LauncherCommandAdapter.qml` returns the single request-plus-persistence outcome from direct and dispatched pin/unpin commands; tests cover structured success and adapter return shape. Verified with `node tests/taskactionlogic.test.mjs && node tests/launchercommandadapterqml.test.mjs && node tests/launchersynclogic.test.mjs && node tests/launchersyncadapterqml.test.mjs`, `just check`, `just test-host`, and `just lint-host`. Commit: 01d7fbb, cfce925.
- P2 launcher sync retry classification: launcher sync failures now carry explicit `retryClassification`, reconciliation branches on that classification instead of raw `write-mismatch` code checks, and sync warnings include retry classification; tests cover write mismatch, write failure, and reconciliation expiry. Verified with `node tests/launcherlistlogic.test.mjs && node tests/launchersynclogic.test.mjs && node tests/launchersyncadapterqml.test.mjs`, `just check`, `just test-host`, and `just lint-host`. Commit: 5e99cae, 77cc39b.
- P1 normal task source lifecycle: `NormalTaskSourceLifecycleLogic.mjs` now owns normal task row appeared, changed, and removed publication decisions while `NormalTaskSource.qml` projects live roles and executes returned lifecycle commands; pure and QML source-shape tests cover the normal source lifecycle boundary. Verified with `node tests/normaltasksourcelifecyclelogic.test.mjs && node tests/normaltasksourceqml.test.mjs && node tests/normaltaskstorelogic.test.mjs && node tests/taskmodellogic.test.mjs`, `just check`, `just test-host`, and `just lint-host`. Commit: 78d923c, 0a294cd.
- P1 remote attention source lifecycle: `RemoteAttentionLogic.mjs` now owns remote attention row appeared, changed, removed, key-change, and became-qualified transition decisions while `RemoteAttentionSource.qml` projects live roles and executes returned lifecycle commands; pure and QML source-shape tests cover target ordering and removal of delegate-local transition state. Verified with `node tests/remoteattentionlogic.test.mjs && node tests/remoteattentionsourceqml.test.mjs && node tests/visibletaskitemslogic.test.mjs && node tests/taskactivationadapterqml.test.mjs`, `just check`, `just test-host`, and `just lint-host`. Commit: a90aef6, 8de48d4.
- P2 normal source launcher boundary: `NormalTaskSource.qml` now derives launcher position and pinned state from the injected `visibleLauncherPosition` callback instead of importing launcher-list policy or reading raw launcher model state; QML source tests cover the boundary. Verified with `node tests/normaltasksourceqml.test.mjs && node tests/taskplatformstateqml.test.mjs && node tests/normaltasksourcelifecyclelogic.test.mjs && node tests/normaltaskstorelogic.test.mjs`, `just check`, `just test-host`, and `just lint-host`. Commit: 459ce39, 308bbe5.
- P1 launcher sync port boundary: implemented `LauncherSyncPort.qml` for launcher-list and configuration read/write effects and switched `LauncherSyncAdapter.qml` plus root wiring to consume it instead of raw `TasksModel`; documented the port boundary in architecture and added QML source-shape coverage. Verified with `node tests/launchersyncadapterqml.test.mjs && node tests/launchercommandadapterqml.test.mjs && node tests/taskmoveadapterqml.test.mjs`, `just check`, `just test-host`, and `just lint-host`. Commit: 5654d15, 290d708, b81b097.
- P1 context-menu role port boundary: implemented `TaskContextMenuRolePort.qml` for task role validation and live role reads, switched `TaskContextMenuAdapter.qml`, `TaskContextMenu.qml`, `TaskContextMenuRoleState.qml`, root wiring, and remote-attention menu requests to pass role ports instead of broad task models; documented the role-port boundary and added focused QML/pure tests. Verified with `node tests/taskcontextmenuadapterqml.test.mjs && node tests/taskcontextmenulogic.test.mjs && node tests/remoteattentionsourceqml.test.mjs && node tests/taskactionlogic.test.mjs && node tests/taskcontextmenurolelogic.test.mjs`, `just check`, `just test-host`, and `just lint-host`. Commit: d3d0479, dd12df3, 56cb1ae.
- P2 desktop action descriptor seam: implemented internal `DesktopActionDescriptor` extraction and descriptor-to-`QAction` adaptation in `TaskContextMenuBackend`, leaving launch failure observability and executable C++ fake-input coverage as residual backend work; documented the descriptor boundary and added source-shape coverage. Verified with `node tests/taskcontextmenubackendcpp.test.mjs`, `just check`, `just test-host`, and `just lint-host`. Commit: 79ff057, 8da98f5, b2afd75.
